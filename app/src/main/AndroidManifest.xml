<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppointmentSystem"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".DoctorDashboardActivity" />
        <activity android:name=".DoctorProfileSetting" />
        <activity android:name=".DoctorAppointments" />
        <activity android:name=".DoctorScheduleSetActivity" />
        <activity android:name=".PatientDashboardActivity" />
        <activity android:name=".PatientProfileSetting" />
        <activity android:name=".PatientAppointments" />
        <activity android:name=".DoctorProfileActivity" />
        <activity android:name=".DoctorsListActivity" />
        <activity android:name=".DoctorsAdapter" />
    </application>
</manifest>
